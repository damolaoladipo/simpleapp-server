export enum ENVType {
    PRODUCTION = 'production',
    STAGING = 'staging',
    DEVELOPMENT = 'development'
    }

    export enum UserType {
        SUPER = "superadmin",
        ADMIN = 'admin',
        USER = 'user'
    }